buildscript {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' } // INI WAJIB
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0'
        classpath 'com.highcapable.yukihookapi:plugin:1.1.0'
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' } // INI JUGA WAJIB
    }
}